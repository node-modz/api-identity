overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/graphql/accounting/*.graphql"
generates:
  src/generated/accounting/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
