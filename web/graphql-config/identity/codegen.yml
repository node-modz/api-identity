overwrite: true
schema: "http://localhost:4000/graphql"
documents: "graphql-config/identity/*.graphql"
generates:
  src/graphql/identity/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
