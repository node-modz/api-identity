overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/graphql/identity/*.graphql"
generates:
  src/generated/identity/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
